# GUIDE.md —— 2026 高考志愿填报智能工具箱 · 新手操作指南

> 本指南面向**完全不懂本项目的零基础新手**，手把手教你把"高考分数"变成一份可提交的 96 志愿草稿。
> 适用对象：重庆物理类考生（本项目默认档案为 581 分 / 位次 20161 / 物化生 / 彭水少数民族考生）。
> 只要你会用浏览器、会填表单、会按按钮，就能照着这份指南一步步做完。

---

## 目录

- [0. 一句话说清这工具能做什么](#0-一句话说清这工具能做什么)
- [1. 前置准备：开始前你要备齐的 8 类信息](#1-前置准备开始前你要备齐的-8-类信息)
- [2. 两种使用方式](#2-两种使用方式)
- [3. 七步操作指南（逐分区讲解）](#3-七步操作指南逐分区讲解)
- [4. 结果解读：怎么读懂颜色、风险灯、概率与置信度](#4-结果解读怎么读懂颜色风险灯概率与置信度)
- [5. 置信度四档说明](#5-置信度四档说明)
- [6. 96 志愿定稿检查清单](#6-96-志愿定稿检查清单)
- [7. 常见问题 FAQ](#7-常见问题-faq)
- [8. 重要提醒](#8-重要提醒)

---

## 0. 一句话说清这工具能做什么

**这是一个纯静态网页工具箱，帮重庆物理类考生把"分数+位次"换算成历年可比口径，自动分出冲/稳/保三档志愿，逐条查防退档风险，识别大小年，挖同层次套利机会，最后汇总成一份带风险标注的 96 志愿候选池。** 适合想自己动手填志愿、又怕踩坑的考生和家长。

它**不替代**官方志愿填报系统，所有结论**仅供参考**（详见第 8 节）。

---

## 1. 前置准备：开始前你要备齐的 8 类信息

打开工具页之前，请先把下面 8 类信息查清楚。每一项后面都写了"为什么需要"——漏掉任何一项，下游某个分区就会降级或报错。

| 序号 | 要准备的信息 | 在工具里填到哪 | 为什么需要 |
|---|---|---|---|
| 1 | **高考分数**（实考分，整数） | 区1 档案区「高考分数」框 | 位次换算、梯度分档、套利评分的基准输入 |
| 2 | **全省位次**（一分一段表查到的累计排名） | 区1 档案区「全省位次」框 | 比分数更准的竞争力指标，Skill 1/2/B 全靠它 |
| 3 | **选考科目**（物/化/生/政，勾选） | 区1 档案区「选科」复选框 | Skill 3 防退档会用它卡"选科不符"硬错误 |
| 4 | **户籍**（区县 + 城镇/乡村） | 区1 档案区「户籍（彭水）」下拉 | 决定能否走预科班、国家专项、地方专项 |
| 5 | **少数民族身份**（是/否 + 具体民族） | 区1 档案区「少数民族」下拉 | 预科班与加分资格的前置条件 |
| 6 | **体检结论**（正常 / 色盲色弱 / 待确认） | 区1 档案区「体检」下拉 | 医学、化学类专业会因色盲色弱被退档 |
| 7 | **家庭经济是否受限**（否 / 是，"是"=禁中外合作） | 区1 档案区「经济受限」下拉 | 中外合办学费年均 6-10 万，会触发软警告 |
| 8 | **报考诉求**（考研否、体制内、国企央企、南北方、是否一定要 211） | 自己心里有数，用来筛选志愿池 | 决定你往冲/稳/保三档里塞哪些院校专业 |

### 逐项解释

- **分数与位次**：重庆是"专业+学校"模式，位次比分数更关键。本项目默认档案是 581 分 / 位次 20161，两个数都填进区1，工具会以位次为主进行换算。查位次的方法：在重庆市教育考试院发布的"一分一段表"里，用你的分数往下累加同分人数，得到的就是你的累计位次。
- **选科**：物化生是默认组合。若你没选政治，公安、法学部分方向会被 Skill 3 标"选科不符"硬错误。勾错会导致整池志愿被错误拦截，务必如实勾。
- **户籍**：彭水苗族土家族自治县属于民族自治地方，能走预科班和国家专项；如果户籍是"乡村"，还能走地方专项。下拉里有"彭水·待确认/彭水·乡村/彭水·城镇"三档，不确定就先留"待确认"（部分资格会标"需核实"）。
- **少数民族**：默认"是"。彭水聚居地少数民族符合预科班资格，也符合 +10 分加分（投档分）。
- **体检**：默认"待确认"。如果你报了临床医学、口腔、药学、化学等专业且体检是色盲色弱，Skill 3 会直接标红 error。**强烈建议正式填报前去医院/学校确认体检结论**。
- **经济受限**：默认"否"。选"是"后，任何"中外合作"专业都会触发 warn（软警告，不阻断但提示）。
- **报考诉求**：本项目档案的诉求是「不考研 / 体制内 / 国企央企 / 南方优先 / 尽量 211」，主线方向是「计算机网络 / 财会 / 电气 / 预科」。你可以照搬，也可以按自己情况在脑中替换——这决定了你看区2 冲稳保结果时优先保留哪一类。

> **查不到的信息怎么办**：留"待确认"或"unknown"，工具会把对应项标成 info（提示性，不阻断），但**该志愿不能作为最终保底**（`canUseAsSafe=false`）。补全前别拿它兜底。

---

## 2. 两种使用方式

> 页面顶部除「档案区」外，还内置一个「新手向导 · Wizard」分区（`id="wizard"`），把全流程串成 5 步对话式表单。下面"方式一"是**跟着向导走**（推荐新手）；"方式二"是熟悉后跳着用 6 大分区的进阶路径。

### 方式一：新手向导式（推荐，照着本指南走）

适合第一次用、对志愿填报流程没概念的人。页面顶部「新手向导 · Wizard」分区（`id="wizard"`）把全流程拆成 5 步对话式表单，填完点「⚡ 生成志愿方案」即可一键跑完全部 5 个 Skill，不必手动逐块操作 6 大分区。

向导 5 步流程（每步对应页面上一个 `w-step-*` 卡片）：

| 步骤 | 在问什么 | 你要填的控件 | 答完自动出的结论 |
|---|---|---|---|
| 第1步 基本信息 | 你的分数与位次口径 | 分数 `w-score`、位次 `w-rank`、选科类 `w-category`、特控线 `w-scl` | 锁定考生基准档案 |
| 第2步 选科与身份 | 你的选考科目与身份资格 | 选考科目 `w-sub-*`（物/化/生/政/史/地）、民族 `w-ethnicity`、户籍 `w-hukou`、体检 `w-physical` | Skill 3 防退档的资格校验输入 |
| 第3步 经济与诉求 | 你的经济约束与报考偏好 | 经济是否受限 `w-economic`、报考诉求多选 `w-goal`、意向方向多选 `w-line` | 志愿池筛选与套利方向 |
| 第4步 确认信息 | 核对汇总 | 勾「我已核对以上信息准确」`w-confirm`（汇总显示在 `w-confirm-summary`） | 解锁第5步生成按钮 |
| 第5步 生成方案 | 一键跑完 5 个 Skill | 点「⚡ 生成志愿方案」（末步 `w-next` 按钮文字变为生成） | 在 `w-result` 区输出带置信度徽章的五块结论 + 底部三条最重要建议 |

**怎么走**：用「上一步 `w-prev` / 下一步 `w-next`」按钮依次推进，进度条 `w-progress` 实时显示「第 N / 5 步」。第5步会自动按 **Skill 1 → Skill 2 → Skill 3 → Skill A → Skill B** 顺序调用，在 `w-result` 区输出五块结论（每块标题旁挂置信度徽章：极高/高/中/低）+ 底部「三条最重要建议（置信度高的优先）」+ 官方系统提交提醒。档案没填对，向导结果会降级，务必如实填。

### 方式二：按顺序手动操作 6 大分区（进阶）

适合已经看过一遍指南、想快速复查或只跑某个 skill 的人。直接跳到第 3 节对应分区即可。注意：**区3 梯度、区4 防退档、区7 仪表盘都依赖区1 档案**，跳着用前先确认区1 已填对。

---

## 3. 七步操作指南（逐分区讲解）

> 编号说明：页面整体结构是**顶部档案区 + 新手向导 + 6 个分区面板**。本指南按操作顺序编为**区1 ~ 区7**：**区1 是页面顶部的档案输入区**（hero），**区2 ~ 区7 对应页面自上而下的 6 个分区面板**——页面自身的角标是「区1·位次归一化」~「区6·综合仪表盘」，比本指南少 1。新手可先走顶部的「新手向导」（见下方第 0 步），熟悉后再按区1~区7 逐块手动操作。每步都标注了页面锚点 `id`，浏览器地址栏输 `tools.html#rank` 可直接跳到对应位置。

### 第0步 · 新手向导（推荐新手使用）

如果你是第一次用、不想一块块手动操作 6 大分区，直接走页面顶部的「新手向导 · Wizard」分区（`id="wizard"`，位于档案区下方）。它把全流程拆成 5 步对话式表单：

1. **第1步 基本信息**：填分数 `w-score`、位次 `w-rank`、选科类 `w-category`、特控线 `w-scl`。
2. **第2步 选科与身份**：勾选考科目 `w-sub-*`（物/化/生/政/史/地），选民族 `w-ethnicity`、户籍 `w-hukou`、体检 `w-physical`。
3. **第3步 经济与诉求**：选经济是否受限 `w-economic`，勾报考诉求 `w-goal` 与意向方向 `w-line`（均多选）。
4. **第4步 确认信息**：在 `w-confirm-summary` 核对汇总，勾「我已核对以上信息准确」`w-confirm`。
5. **第5步 生成方案**：点「⚡ 生成志愿方案」（末步 `w-next` 按钮文字变为生成），自动按 **Skill 1 → Skill 2 → Skill 3 → Skill A → Skill B** 顺序调用，在 `w-result` 区输出带置信度徽章（极高/高/中/低）的五块结论 + 底部三条最重要建议。

用 `w-prev` / `w-next` 按钮切换步骤，进度条 `w-progress` 显示「第 N / 5 步」。填完即可一键拿到与 6 大分区等价的综合结论，不必再手动逐块跑。

> 下方区1~区7 为**进阶方式**：适合想单独复查某个 Skill、或跳着用 6 大分区的用户。新手走完第0步向导即可拿到等价结论，不必逐块手动操作。

### 区1 · 考生档案输入

- **页面位置**：顶部 HERO 卡片区（`id="profile"`）。
- **目的**：把第 1 节备齐的 8 类信息录入系统，作为所有 skill 的输入基准。改任一字段会触发全表重算。
- **输入什么**（对应页面 `profile-strip` 里的格子）：

| 字段 | 控件类型 | 默认值 | 怎么填 |
|---|---|---|---|
| 高考分数 | 数字框 `f-score` | 581 | 填你的实考分 |
| 全省位次 | 数字框 `f-rank` | 20161 | 填一分一段表查到的累计位次 |
| 选科（物化生） | 复选框 `f-sub-*` | 物√化√生√政□ | 勾你实际选考的科目 |
| 少数民族 | 下拉 `f-ethnicity` | 是 | 是/否 |
| 户籍（彭水） | 下拉 `f-hukou` | 彭水·待确认 | 待确认/乡村/城镇 |
| 体检 | 下拉 `f-physical` | 待确认 | 待确认/正常/色盲色弱 |
| 经济受限 | 下拉 `f-economic` | 否 | 否/是（禁中外合作） |

- **输出什么**：没有独立输出，但所有下游分区会立刻用这些值重算。改完分数/位次，区2 等位分表、区3 三桶、区7 仪表盘都会变。
- **怎么看结果**：填完后往下滑，区2 表格应该自动出现 2025 年等位分；如果空白，说明数据没加载（见 FAQ Q1）。
- **常见坑**：
  - 位次填错（把分数当位次）：下游全错。重庆物理类位次通常是 5 位数，分数是 3 位数，别搞混。
  - 选科勾多/勾少：会把不该报的专业放行，或把能报的专业误拦。
  - 户籍留"待确认"就想走预科/专项：资格校验会标"需核实"，不能当定稿。

### 区2 · 位次换算（Skill 1 · RankNormalizer）

- **页面位置**：页内标题「区1 · 位次归一化 / Skill 1」（`id="rank"`）。
- **目的**：把"今年的分数/位次"换算成历年可比的"等位分"，识别"名义分 vs 真实竞争力"。比如 581 分在 2025 位次 20161，等位分约 576 分——意味着你的分数在 2025 口径下只值 576 分。
- **输入什么**：
  - 「实考分」数字框 `r-score`（默认 581，随区1 同步）。
  - 「位次（取自档案）」只读显示 `r-rank-show`（默认 20161，来自区1）。
  - 「目标年份」三个复选框：2025√ / 2024√ / 2023√（默认全勾）。
- **输出什么**：
  - `rank-table` 表格，4 列：**年份 / 等位分 / 等位位次 / Δ（等位分−实考分）**。
  - `rank-chart` 双 Y 轴折线图：等位分与等位位次的跨年趋势。
  - 若 2024/2023 数据未补录，会出现黄色 `callout warn`：「2024/2023 等年份一分一段表待补录，等位分暂不可用；Δ 列因 2026 基准表未发布暂为空。当前仅 2025 年有锚点数据。」
- **怎么看结果**：
  - 看 **Δ 列**：正数表示"通胀"（目标年等位分高于你的实考分，即你的分数在目标年更值钱），负数表示"通缩"。本项目当前只有 2025 锚点，Δ 多半为空——属正常降级，不是 bug。
  - 看 **等位分**：如果显示具体数值（如 576.2），说明是 2025 真实锚点算出来的，可信度高；显示空或"待补录"，是数据缺口。
- **对应 API**（浏览器 console 可调，需先打开 tools.html）：
  - `GK.Skills.RankNormalizer.scoreToRank(2025, 'physics', 581)` → 18260（分数转位次）
  - `GK.Skills.RankNormalizer.rankToScore(2025, 'physics', 20161)` → 约 576.2（位次转分数，bisect 二分 + 线性插值）
  - `GK.Skills.RankNormalizer.normalize(20161, {candidateYear:2026, targetYears:[2025,2024,2023], category:'physics'})` → 多年等位分对照
- **常见坑**：
  - 把"等位分"当成"预测录取分"：等位分只是口径换算，不是该校录取线。
  - 期待 2024/2023 有数：当前数据集只有 2025 真实锚点，其余年份待补录，会返回 `null`。

### 区3 · 梯度模型（Skill 2 · VolunteerOptimizer）

- **页面位置**：页内标题「区2 · 冲稳保梯度 / Skill 2」（`id="gradient"`）。
- **目的**：把志愿池按位次系数 `c = 院校位次 / 考生位次` 分进冲/稳/保三桶，并给出每条志愿的录取概率。
- **输入什么**：
  - `grad-pool`：志愿池（来自数据集 `GK.Data.Schools`，48 条候选）。
  - 三个滑块（`coef-rush` / `coef-steady` / `coef-safe`）：
    - 冲 rush：范围 0.80 ~ 0.95，默认 **0.85**
    - 稳 steady：固定 **1.00**（不可调）
    - 保 safe：范围 1.10 ~ 1.30，默认 **1.15**
- **分档规则**（位次系数 c = schoolRank / candidateRank，c 越小学校越强）：

| 区间 | 档位 | 中文 | 含义 |
|---|---|---|---|
| c < 0.85 | hopeless | 渺茫 | 学校过强，基本没戏 |
| 0.85 ≤ c ≤ 0.95 | surge | 冲 | 冲刺层，概率 20%~50% |
| 0.95 < c < 1.00 | gap | 缝隙 | 略冲，建议谨慎 |
| 1.00 ≤ c ≤ 1.15 | stable | 稳 | 稳妥层，概率 70%~95% |
| 1.15 < c ≤ 1.30 | safe | 保 | 保底层，概率 95%~99% |
| c > 1.30 | overSafe | 过保 | 分数浪费，建议替换 |

> 注：`gap`（缝隙）、`hopeless`（渺茫）、`overSafe`（过保）三类会落进 `dropped` 桶，不进冲稳保。

- **输出什么**：
  - `grad-tiers`：三张彩色汇总卡（rush 红 / stable 蓝 / safe 青绿），显示每桶数量与代表院校。
  - `grad-table`：每条志愿的档位徽章 + 录取概率 + 位次差。
  - 若有剔除，出现黄色 `callout warn`：「⚠️ 已剔除 X 条志愿（落入 hopeless/gap/overSafe 或缺数据）」。
- **怎么看结果**：
  - 看三张卡的**数量分布**：理想是冲少、稳多、保有底。本工具建议配额是 **冲 15 / 稳 55 / 保 26**（呼应重庆本科批 96 平行志愿）。
  - 看每条的**录取概率 prob**：冲档 20%~50%、稳档 70%~95%、保档 95%~99%。低于 20% 的冲基本是陪跑。
  - 滑块怎么调：想更激进就把 `coef-rush` 往 0.95 拖（更多志愿算"冲"）；想更保守就把 `coef-safe` 往 1.10 拖（更多志愿算"保"）。`steady` 锁死 1.00 不可动。
- **对应 API**：
  - `GK.Skills.VolunteerOptimizer.classifyTier(20161, 18260, {})` → `{tier:'surge', prob:..., marginRank:1901, ...}`（marginRank = candidateRank − schoolRank = 20161 − 18260，正数表示院校位次靠前、属"冲"类）
  - `GK.Skills.VolunteerOptimizer.optimizeBucket(GK.Data.Candidate, GK.Data.Schools, {coef:{rush:0.85,steady:1.0,safe:1.15}})` → `{rush:[...], stable:[...], safe:[...], dropped:[...]}`
  - `GK.Skills.VolunteerOptimizer.suggestQuota([], {total:96})` → `{rush:15, steady:55, safe:26, ...}`
- **常见坑**：
  - 以为"冲"越多越好：冲档录取率低，堆太多会挤占稳/保名额，导致无兜底。
  - 忽略 `dropped` 桶：被剔除的志愿里可能有"过保"（分数浪费）和"缝隙"（略冲），值得回头看一眼。
  - 改了滑块没重算：拖动滑块会自动重算，若没反应刷新页面再试。

### 区4 · 防退档校验（Skill 3 · AdmissionSafety）

- **页面位置**：页内标题「区3 · 防退档校验 / Skill 3」（`id="safety"`）。
- **目的**：逐条志愿过规则引擎，查"选科/体检/校区/学费/预科资格/已剔除院校"等硬伤，避免填了等于白填。
- **输入什么**：
  - 复选框 `safe-link`（默认√）：**联动区2 志愿池**——把区3 分好桶的志愿拿过来校验。
  - 复选框 `safe-removed`（默认√）：**附加已剔除院校核验**（南农/东林/川农/贵大/广大，这些经核实未在渝招生，误填直接 error）。
  - 按钮 `safe-rerun`「重新校验」：手动触发一次校验。
- **校验项**（`checkByRule` 支持的规则名）：

| 规则名 | 检查内容 | 触发后果 |
|---|---|---|
| `subject_mismatch` | 选科是否满足专业要求 | error（硬拦截） |
| `single_subject_limit` | 单科成绩限制 | warn（需核实） |
| `physical_limit` | 体检结论（色盲色弱） | error / info |
| `campus_trap` | 独立学院/异地分校 | warn |
| `tuition_conflict` | 中外合作 + 经济受限 | warn |
| `hukou_ineligible` | 户籍不符专业要求 | error |
| `precollege_ineligible` | 预科班需民族自治地方户籍 | error |
| `specialplan_ineligible` | 国家专项/地方专项资格 | error |
| `data_missing` | 体检未知 + 医学化学专业 | info |
| `removed_school` | 院校在 removed 清单 | error（riskScore=100） |

- **输出什么**：
  - `safe-banner`：顶部统计条——「共校验 X 条志愿：N 个 error（必须处理）/ M 个 warn / K 个 info」。
  - `safe-table`：5 列表格——**院校 / 专业 / 风险灯 / 问题数 / 详情**。点"详情"展开每条 issue 的 `callout`（danger 红 / warn 黄 / info 蓝）。
  - 风险灯 `t-risk-dot`：绿色=低风险(low)、黄色=中风险(mid)、红色=高风险(high)。
- **风险评分规则**：`riskScore = max(硬错误分, 5) + min(25, 软警告累计)`，clamp 0~100。<30 为 low，<75 为 mid，否则 high。`canUseAsSafe=true` 仅当无硬错误且 riskScore<30。
- **怎么看结果**：
  - 先看 banner：**有 error 必须处理**（标红的志愿要么删要么改，不能留）。
  - 再看每条风险灯：**绿灯**才能当保底（`canUseAsSafe=true`）；**黄灯**可留但心中有数；**红灯**必须处理。
  - 展开"详情"读 issue：每条 callout 写明了哪条规则、什么问题、怎么改。
- **对应 API**：
  - `GK.Skills.AdmissionSafety.checkVolunteer({school:'南京农业大学', policy_tag:'少数民族预科班', subjects_required:['物理','化学']}, GK.Data.Candidate)` → `{passed:false, risk:'high', riskScore:100, issues:[...], canUseAsSafe:false}`
  - `GK.Skills.AdmissionSafety.checkBatch(volunteers, GK.Data.Candidate)` → 批量结果
  - `GK.Skills.AdmissionSafety.checkEligibility(GK.Data.Candidate, 'precollege')` → `{eligible:true, missing:[], note:'彭水聚居地少数民族符合预科班资格'}`
    - `planType` 可选：`'precollege'` / `'national_special'` / `'local_special'` / `'minority_bonus'`
- **常见坑**：
  - 体检留"待确认"就报医学专业：会标 info（不阻断），但**该志愿不能当保底**，正式填报前必须补体检结论。
  - 以为预科班谁都能报：预科班仅面向民族自治地方（彭水等）少数民族，户籍不符直接 error。
  - 忽略 removed 清单：南农、东北林、川农预科经核实未在渝 2025 招生，误填会被 Skill 3 拦截——这是本工具帮你避的最大的坑之一。

### 区5 · 大小年识别（Skill A · VolatilityDetector）

- **页面位置**：页内标题「区4 · 大小年波动 / Skill A」（`id="volatility"`）。
- **目的**：用某专业历年位次序列算标准差与变异系数 CV，识别"大年（分高难考）/小年（分低易考）"的振荡规律，避免追涨、寻找抄底。
- **输入什么**：
  - 下拉 `vol-select`「选择院校专业」：从院校池里挑一个想查的专业（数据来自 `GK.Data.Schools` 的 `history_ranks` 字段）。
- **输出什么**：
  - `vol-chart`：历年录取位次变化折线图（大小年不同色）。
  - `vol-stats`：统计指标网格——年数 `years`、均值 `mean`、样本标准差 `std`、变异系数 `cv`、波动等级 `level`、趋势 `trend`。
  - `vol-conclusion`：文字结论。
- **波动等级判定**（`level`）：

| level | 条件 | 含义 |
|---|---|---|
| `low` | cv < 0.03 | 位次稳定 |
| `mid` | 0.03 ≤ cv ≤ 0.08 | 波动中等 |
| `high` | cv > 0.08 | 波动较大，建议谨慎 |
| `insufficient` | years < 3 | 数据不足，无法判定 |

- **趋势 `trend`**：`up`（位次变小=分上涨）/ `down`（位次变大=分下降）/ `flat` / `unknown`。
- **大小年判据**（`detectBigSmallYear`，三条件全满足才判定）：a. cv > 0.15；b. currentRank > lastRank × 1.2；c. 振荡反转次数 ≥ 2。
- **怎么看结果**：
  - `level='high'` 的专业：位次不稳，今年好考不代表明年好考，谨慎追。
  - `level='insufficient'`：数据不够，**不要凭空脑补趋势**，等补录。
  - 若 `detectBigSmallYear` 返回 `isBigSmallYear:true`：说明检测到大小年，可参考 `predictNext`（预测明年大/小年）择时，但置信度看 `confidence` 字段，别盲信。
- **对应 API**：
  - `GK.Skills.VolatilityDetector.volatility([{year:2023,rank:18000},{year:2024,rank:22000},{year:2025,rank:20000}])` → `{years:3, mean:20000, std:2000, cv:0.1, level:'high', trend:'up', note:...}`
  - `GK.Skills.VolatilityDetector.detectBigSmallYear(rankSeries, currentRank)` → `{isBigSmallYear, pattern, lastYear, predictNext, confidence, note}`
  - `GK.Skills.VolatilityDetector.filterByVolatility(volunteers, {minYears:3, maxCV:0.08})` → 按波动过滤志愿
- **常见坑**：
  - 2 年数据就想判大小年：必须 ≥3 年，否则 `level='insufficient'`。
  - 把"波动大"当"机会大"：波动大意味着不确定性高，对保底志愿是负面信号。
  - 部分专业压根没历史位次：会直接 `insufficient`，需补录数据。

### 区6 · 同层次套利（Skill B · ArbitrageDetector）

- **页面位置**：页内标题「区5 · 同层次套利 / Skill B」（`id="arbitrage"`）。
- **目的**：用"位次差换区位"的思路，找"同层次但区位更好"的院校对——比如用内地 211 的位次换沿海双非强校，提升就业区位优势。
- **输入什么**：
  - 下拉 `arb-target`「目标院校」：选一个你想找替代/对标的目标。
  - 下拉 `arb-tier`「层次」：全部 / 211 / 双一流 / 公办 / 市属 / 省属。
  - 下拉 `arb-region`「区位」：不限 / 南方。
  - 按钮 `arb-rerun`「重新检测」。
- **套利对五条件**（`findArbitragePairs`，全满足才配对）：
  1. 两院校位次差 ≤ `pairDelta`（默认 1000）；
  2. 两院校均落在 `[考生位次 - rankWindow, 考生位次 + rankWindow]`（默认窗 2000）；
  3. 专业方向相同（computer / finance / electrical，由专业名关键字推断）；
  4. 跨省（两地省份不同）；
  5. 一冲一保：A 位次靠前（冲）< 考生位次 < B 位次靠后（保）。
- **套利评分**：`score = locationPremium - rankCost`，其中 `rankCost = (rankB - rankA) / candidateRank`，`locationPremium` 按城市层级提升（一线 +0.30 / 新一线 +0.20 / 同级 0）。score ≥ `minScore`（默认 0.05）才输出。
- **输出什么**：
  - `arb-pairs`：套利对卡片列表，每对含 A→B、城市溢价、位次成本、套利分、一句话说明。
  - `arb-table`：套利明细表，按 score 降序。
- **怎么看结果**：
  - 看 `score`：正分越高，套利空间越大（区位提升 > 位次成本）。
  - 看 `locationPremium`：0.30 表示从"其他城市"升到"一线"，0.20 表示升到"新一线"。
  - 看 `note`：会写明 A 校 rank 多少、B 校 rank 多少、溢价多少、成本多少。
- **对应 API**：
  - `GK.Skills.ArbitrageDetector.scoreArbitrage({school:'A校', rank_2025:18000}, 20161)` → `{score:0.1072, reasons:['院校位次比考生靠前 2161 名，属"冲"类套利空间']}`
  - `GK.Skills.ArbitrageDetector.findArbitragePairs(20161, {province:'重庆'}, GK.Data.Schools, {rankWindow:2000, pairDelta:1000, minScore:0.05})` → `[{pair:[A,B], rankCost, locationPremium, score, note}, ...]`
  - `GK.Skills.ArbitrageDetector.findPeers(targetItem, allItems, candidateRank, {rankTolerance:0.15})` → 同方向 + 位次窗内的 peer 院校
  - `GK.Skills.ArbitrageDetector.highlight(volunteers, candidateRank, 5)` → TopN 套利亮点 + 一句话理由
- **常见坑**：
  - 期待同省套利：套利要求**跨省**，同省院校不会配对。
  - 期待任意专业都能配：必须**专业方向同类**（computer/finance/electrical），其它方向不配。
  - 把套利分当录取概率：套利分是"区位性价比"，不是"录取可能性"。

### 区7 · 综合仪表盘

- **页面位置**：页内标题「区6 · 综合仪表盘」（`id="dashboard"`）。
- **目的**：把前 6 区的结果汇总成一份可导出的 96 志愿候选池 + 风险报告，按冲/稳/保三层着色，每条志愿附概率、风险标记、大小年标记。
- **输入什么**：
  - 按钮 `dash-gen`「⚡ 生成综合报告」：一键汇总。
  - 按钮 `dash-export`「导出 JSON」：把报告导出为 JSON 文件，方便存档或二次处理。
- **输出什么**：
  - `dash-overview`：三层着色概览卡（rush 红 / stable 蓝 / safe 青绿），呼应冲 15/稳 55/保 26 的配额。
  - `dash-callouts`：关键提示块（含已剔除院校、高风险志愿、待补录数据等）。
  - `dash-table`：综合志愿表，每条含院校/专业/档位徽章/录取概率/风险灯/大小年标记。
- **怎么看结果**：
  - 先看概览卡**数量**是否接近 15/55/26；不足时仪表盘会提示需补充多少条至满档。
  - 看 `dash-callouts` 里有没有红色 danger 块——有的话必须先回区4 处理 error。
  - 在 `dash-table` 里逐条确认：冲档有概率、稳档概率 ≥70%、保档概率 ≥95% 且风险灯为绿。
- **常见坑**：
  - 没点"⚡ 生成综合报告"就看表：仪表盘不会自动跑，必须手动点一次。
  - 导出 JSON 后不知道怎么用：JSON 是结构化数据，可以用任何文本编辑器打开，或导入表格软件二次筛选；最终志愿仍需在官方系统手动录入。

---

## 4. 结果解读：怎么读懂颜色、风险灯、概率与置信度

### 4.1 冲 / 稳 / 保三色

整个工具用三套颜色贯穿档位标识：

| 颜色 | 档位 | 徽章文字 | 概率区间 | 含义 |
|---|---|---|---|---|
| 红色 | surge | 冲 | 20% ~ 50% | 冲刺，录取不稳 |
| 蓝色 | stable | 稳 | 70% ~ 95% | 稳妥，主力录取区 |
| 青绿色 | safe | 保 | 95% ~ 99% | 保底，兜底防滑档 |
| 灰色 | gap / hopeless | 缝隙 / 渺茫 | — | 已剔除，不进冲稳保 |
| 金色 | overSafe | 过保 | 99% | 分数浪费，建议替换 |

- 汇总卡（`tcard`）：冲红渐变（#b91c1c）、稳蓝渐变（#1e3a8a）、保青绿渐变（#0f766e）。
- 表格徽章（`t-tier-badge`）：同色系小标签。

### 4.2 风险灯（risk dot）

区4 防退档与区7 仪表盘里每条志愿前的圆点：

| 颜色 | risk 等级 | riskScore | 能否当保底 |
|---|---|---|---|
| 绿色 | low | < 30 | 可以（`canUseAsSafe=true`） |
| 黄色 | mid | 30 ~ 74 | 不建议，需心中有数 |
| 红色 | high | ≥ 75 | 不可以，必须处理 |

### 4.3 录取概率（prob）

区3 梯度表与区7 仪表盘都会显示 `prob`（0~1 的小数，或百分比）：

- 概率由 Skill 2 的 logistic 近似正态 CDF 算出（`computeProb`），以院校位次为均值、σ 取波动标准差或院校位次×5%。
- 院校位次比考生靠前（rank 更小）→ 概率低（冲）；靠后 → 概率高（保）。
- 若该专业 `volatility.level='high'`，概率会自动 ×0.8 衰减（不确定性增大）。

### 4.4 问题清单（issues / callouts）

区4 详情展开后每条问题是一个 `callout`：

| 样式 | severity | 含义 | 处理 |
|---|---|---|---|
| 红色 danger | error | 硬拦截 | 必须删/改 |
| 黄色 warn | warn | 软警告 | 建议处理 |
| 蓝色 info | info | 提示性 | 知晓即可 |

### 4.5 置信度标注

6 大分区面板不单独画一个"置信度"徽章，而是通过**多种信号**（风险灯颜色、概率数值、"待补录/insufficient"字样等）体现结果可靠度——见下一节。**新手向导例外**：向导 `w-result` 区会为每块结论显式挂置信度徽章（见 4.6）。

### 4.6 新手向导输出（w-result）

走「新手向导」第5步生成方案后，`w-result` 区会按 **Skill ① → ② → ③ → ④(A) → ⑤(B)** 顺序输出五块结论（位次换算 / 冲稳保梯度 / 防退档校验 / 大小年波动 / 同层次套利），每块标题旁挂置信度徽章（`confBadge`）：

| 徽章文字 | 置信度等级 | 样式 | 含义 |
|---|---|---|---|
| 极高 | extreme | 绿色 | 真实录实数据确定 |
| 高 | high | 蓝色 | 录实数据 + 插值 |
| 中 | mid | 黄色 | 概率模型估算 |
| 低 | low | 灰色 | 数据不足降级 |

五块结论下方附「三条最重要建议」（置信度高的优先排列，常见如处理 error 风险、预科资格、剔除高波动院校、配额提示等），并以红色 callout 提醒：**最终须在省考试院官方系统提交，本工具结果仅供参考**。看到「极高/高」徽章的结论可纳入决策；「低」徽章（如待补录年份、`insufficient` 波动）的结论只能参考，不能当定稿依据。

---

## 5. 置信度四档说明

解读任何一条结论时，请按数据来源与算法性质把它归入下面四档之一。**档位越高越可信**：

| 置信度 | 含义 | 在工具里对应的表现 | 典型场景 |
|---|---|---|---|
| **极高** | 真实录实数据确定 | Skill 1 在 2025 真实锚点上的精确匹配；Skill 3 的 removed 清单拦截 | `scoreToRank(2025,'physics',581)`→18260；南农预科被直接 error |
| **高** | 录实数据 + 插值 | Skill 1 在锚点间的线性插值；Skill 2/3 用 `rank_2025` 真实数据分档/校验 | `rankToScore(2025,'physics',20161)`→约 576.2（锚点间 lerp） |
| **中** | 概率模型估算 | Skill 2 的 logistic 概率；Skill B 的套利评分 | 录取概率 70%、套利分 +0.10 |
| **低** | 数据不足降级 | Skill 1 待补录年份返回 `null`/显示"待补录"；Skill A `level:'insufficient'`；体检 unknown + 医学专业 `data_missing` | 2024/2023 等位分为空、大小年无法判定 |

**怎么用这四档**：
- 看到"待补录"、"insufficient"、"需核实"等字样 → **低置信度**，结论只能参考，不能当定稿依据。
- 看到具体等位分（576.2）、具体概率（0.85）、具体套利分（+0.10） → **中或高置信度**，可纳入决策。
- 看到 removed 清单拦截、选科不符硬错误 → **极高置信度**的"不可填"结论，直接删。

---

## 6. 96 志愿定稿检查清单

提交到官方系统前，逐项打勾。任何一项不过都别提交：

- [ ] **1. 区1 档案全部填实**：分数、位次、选科、户籍、体检、经济六项无"待确认/unknown"。体检结论尤其要落实。
- [ ] **2. 区4 防退档 0 个 error**：`safe-banner` 显示"N 个 error"必须为 0；所有黄灯志愿已知晓并接受。
- [ ] **3. 保底档全部绿灯**：保底层（safe）每条风险灯为绿色（`canUseAsSafe=true`），且录取概率 ≥ 95%。保底是兜底，不能有任何硬伤。
- [ ] **4. 冲稳保配额接近 15/55/26**：区7 仪表盘概览卡数量接近冲 15 / 稳 55 / 保 26，不足时按提示补满 96 条。
- [ ] **5. 大小年已排查**：区5 对意向专业逐个查过 `level`；`high` 波动的冲档志愿已谨慎对待，`insufficient` 的不凭空脑补趋势。
- [ ] **6. 已剔除院校未误填**：南农、东北林、川农预科、贵大、广大等 removed 清单内的院校/方向，确认未出现在最终志愿表（Skill 3 会拦，但人工再核一遍）。

---

## 7. 常见问题 FAQ

**Q1：双击 tools.html 打开后，表格是空的 / 报数据加载失败？**
A：`tools.html` 用 `<script src>` 注入 `data/*.js`（不是 fetch），多数浏览器在 `file://` 协议下能加载，但 Chrome 等浏览器默认限制 `file://` 跨目录子资源访问，会导致数据脚本加载失败。改用本地 HTTP 服务：在项目根目录执行 `python3 -m http.server 8000`，然后浏览器访问 `http://localhost:8000/tools.html`。

**Q2：我是别的省份/别的选科，能用吗？**
A：当前数据集只覆盖**重庆物理类**（2025 真实一分一段表 + 48 条院校专业录实）。别的省份/历史类等位分换算会因一分一段表缺失而降级。选科不同可直接在区1 改复选框，Skill 3 会按你的选科重判"选科不符"。

**Q3：2026 年的分数线能用本工具直接算吗？**
A：不能直接用。本仓库数据基准是 **2025 录实数据**，2026 年招生计划与分数线以重庆市教育考试院公布为准。Skill 1 的等位分换算是把"2025 录实"映射到"历年可比口径"，缩小年度偏差，但不等于 2026 实际录取线。

**Q4：录取概率 70% 是不是代表十拿九稳？**
A：不是。概率是 Skill 2 用 logistic 近似正态 CDF 估算的，属"中置信度"。真实录取受招生计划、报考热度、政策调整影响，70% 意味着"位次上算稳妥"，不等于"一定录"。务必配足保底。

**Q5：为什么有些专业没有大小年标记？**
A：Skill A 需要至少 3 年位次数据。`data/schools.js` 中部分专业缺历年位次，会返回 `level:'insufficient'`，需补录数据后才能判定。这不是 bug，是数据缺口。

**Q6：套利检测为什么找不到我想要的院校对？**
A：套利对必须同时满足五条件：位次窗内、同专业方向、跨省、一冲一保、套利分 ≥ 0.05。少一条都不出。常见原因是目标院校与候选不跨省，或专业方向不同类（computer/finance/electrical）。可调宽 `rankWindow` 或换目标院校再试。

**Q7：导出的 JSON 怎么变成志愿表？**
A：JSON 是结构化数据。可用任何文本编辑器打开查看，或用表格软件（Excel/WPS）通过"数据→从 JSON 导入"转成表格二次筛选。**最终志愿必须手动录入官方志愿填报系统**，本工具不替代官方系统。

**Q8：算法结果能保证录取吗？**
A：不能。5 个 skill 的概率模型、波动判定、套利匹配结果仅供参考，不构成志愿填报承诺。本工具箱用于辅助决策与风险自查。

---

## 8. 重要提醒

- **数据仅供参考**：本仓库院校分数线、位次、政策数据均为 **2025 年录实数据**，2026 年招生计划与分数线以**重庆市教育考试院公布为准**。
- **不替代官方系统**：本工具箱用于辅助决策与风险自查，**不能替代**重庆市普通高校招生志愿填报官方系统。最终志愿必须通过官方系统提交确认。
- **重庆"专业+学校"模式**：1 专业 + 1 学校 = 1 志愿，**无校内调剂**。不想去的专业绝对不要填——填了可能被录到那里。
- **已剔除院校**：南京农业大学、东北林业大学、四川农业大学预科班经核实未在渝 2025 招生（系他省数据误传），已计入 `data/policies.js` 的 removed 清单，Skill 3 会拦截误填。
- **置信度自检**：定稿前对照第 5 节四档，确认保底志愿均达"高/极高"置信度，低置信度结论不进入最终保底层。

---

> 本指南与 [README.md](README.md) 互补：README 偏开发者/API 速查，本指南偏新手操作。两者数据基准、API 签名一致，可交叉对照。
